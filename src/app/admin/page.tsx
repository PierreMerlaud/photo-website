"use client";  // Ce composant s'exécute côté client (Upload Image)

import UploadImage from "../components/layout/UploadImage";

export default function AdminPage() {
  return (
    <div>
      <h1>todo = future page admin</h1>
      <UploadImage />
    </div>
  );
}
